import { CSSProperties, ReactNode } from "react";

interface textPropTypes {
    className?: string;
    style?: CSSProperties;
    id?: string;
    children?: ReactNode;
}

export type { textPropTypes };
